const API = '/api/storage?'

const StorageStore = {
    state: {
        id: -1,
        hash: null,
        list: [], // all storage list
        data: null, // choose storage data
    },
    mutations: {
        updateStorage(state, data) {
            // update stoage

        },
    },
}

export default StorageStore
